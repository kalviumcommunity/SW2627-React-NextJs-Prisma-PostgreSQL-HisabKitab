Hisab KitabProduct Requirements DocumentDigital Khatabook / Ledger App for ShopkeepersVersion 1.0  ·  Draft for ReviewPrepared: July 20261. Overview & PurposeHisab Kitab is a digital khatabook (ledger book) application built for shopkeepers who currently track customer and vendor credit on paper. It replaces the handwritten ledger with a real-time, multi-user, auditable digital system, while preserving the simplicity shopkeepers are used to.This document defines the product scope, user flows, and functional requirements for the first shippable version (MVP) as well as the extended modules (Worker & Salary Management, Inventory & Loss Tracking, and AI-Assisted Paper Khata Migration) that are planned as part of the same build effort.This PRD is derived from three source documents already produced for this project: the Feature Spec & Design Rationale, the Application Flow document, and the Project Folder Structure document. It consolidates them into a single reference intended for both technical and non-technical stakeholders.2. Problem StatementShopkeepers who extend credit to regular customers, and who themselves buy on credit from vendors, currently track this using a handwritten paper khata (ledger book). This creates several recurring problems:Balances are not visible in real time, and there is no shared view when more than one person (e.g. an owner and a staff member) manages the same shop.Paper records are error-prone, easy to lose or damage, and offer no audit trail — it is not possible to see who changed an entry, when, or what it looked like before.There is no way to search, filter, or generate a shareable statement of dues without manually copying entries.Migrating years of handwritten history to any digital tool is a large manual effort, which is one of the biggest reasons shopkeepers delay adopting digital tools altogether.Hisab Kitab is being built to solve this with a real-time, multi-user, auditable digital ledger — plus adjacent modules (worker salary tracking, inventory/loss tracking) that round out the financial picture for a small shop.Note: The scale of this problem has not yet been quantified with market sizing or user research data (e.g. number of target shopkeepers, survey/interview evidence). This PRD proceeds without that evidence, per stakeholder direction, and the problem statement above reflects the qualitative rationale already captured in the Feature Spec document rather than measured data.3. Primary UsersThe product defines two roles within a shop, consistent throughout every flow in this document:RoleDescriptionOwnerFull control of the shop: manages settings, roles, and ShopMembers; approves stock adjustments/losses and salary payments; can view all financial totals and the full audit trail; can delete transactions and manage trash/restore.StaffCan add/edit contacts and transactions, mark attendance, submit stock entries, and use search/import features. Access to financial totals, deletion, and approval actions is configurable per shop by the Owner.A shopkeeper (or staff member) can belong to more than one shop and switch between them; the two roles above apply per-shop, not globally to the user account.4. Tech StackNext.js — full-stack React framework (single codebase for frontend and backend)PostgreSQL — relational databasePrisma — ORM for TypeScriptGCP (Google Cloud Platform) — hosting via Cloud RunGitHub Actions — CI/CD (lint, typecheck, test on every PR; build + deploy on merge to main)5. Scope5.1 In-Scope — MVP (Core Ledger)From the original problem statement:Add transactions per contact, with a race-condition-safe balance updateReal-time running balance (pushed to open screens without a refresh)Paginated transaction history (cursor-based, stays fast at any scale)Edit/delete with full audit trail (soft delete, never a hard delete)Prevention of simultaneous edits on the same transactionAdditional MVP features (in scope for v1):Role-based permissions (Owner/Staff)Due date + reminders per contactSearch & filter transactions (by contact, date range, type, amount)Receipt/photo attachment on a transactionTrash / restore for soft-deleted transactions (7-day grace period)Shareable read-only ledger link + PDF export (per-contact and all-dues summary)Multi-shop support (a user can belong to and switch between multiple shops)Analytics dashboard (money in vs out, top contacts by due, monthly trend)Opening balance / bulk CSV import for contactsIn-app notifications (edits by teammates, due-date reminders)Hindi/English language togglePWA — installable, offline-aware, with an offline write-queueDaily & monthly earnings view5.2 In-Scope — Extended ModulesPlanned as part of the same overall build, using the same underlying patterns as the core ledger (locking, audit trail, soft delete):Worker & Salary Management — attendance tracking, monthly/daily-wage pay calculation, salary paymentsInventory & Loss Tracking — products, batches, stock movements, expiry alerts, a formal approval workflow for adjustments/lossesAI-Assisted Paper Khata Migration — photograph paper ledger pages, AI-assisted extraction, mandatory human review before anything is committed5.3 Out of Scope — v1The following are explicitly deferred beyond v1. This list has been compiled from items the source documents already call out as deferred; it has not been separately reviewed or signed off by stakeholders and should be confirmed before development planning is finalized.SMS/WhatsApp notifications — deferred as a later, paid addition requiring a third-party provider (e.g. Twilio/Gupshup); push notifications via Firebase Cloud Messaging are the v1 channelDated line-item extraction for paper khata migration — v1 extracts only name + final balance per contact; extracting individual dated historical entries (to create backdated transactions) is deferred pending an accuracy spike on real samplesMigration of the expiry-alert cron trigger from a free third-party scheduler (cron-job.org) to GCP Cloud Scheduler — deferred until the project is on GCP billing anyway for other reasons; the API route itself does not changeTwo-directional real-time messaging (e.g. self-hosted Socket.io) — not needed, since all real-time requirements are server-to-browser only, covered by Server-Sent EventsPermanent/hard purge of soft-deleted records — soft delete plus a 7-day restore window satisfies the audit-trail requirement for v1; a harder-confirmed permanent purge is optional/admin-only and not committed to v1⚑ Open item: This out-of-scope list should be reviewed by the product owner and confirmed complete — there may be other features or requests (not currently documented) that should be explicitly excluded from v1 to avoid scope creep.6. User StoriesWritten as Role + Action + Benefit, derived from the application flows already defined for this product.6.1 MVP User StoriesAuthentication & OnboardingAs an Owner, I want to register and create my shop in one flow, so that I can start using the ledger without a separate setup step.As an Owner or Staff, I want to stay logged in across sessions, so that I don't have to re-authenticate every time I open the app at the counter.As an new user with zero shops, I want to be guided into shop onboarding automatically, so that I'm never dropped into an empty dashboard without knowing what to do next.Contacts & TransactionsAs an Owner or Staff, I want to add a new contact with an opening balance, so that I can start tracking a customer or vendor from wherever their real balance currently stands.As an Owner or Staff, I want to record a 'You Gave' or 'You Got' transaction against a contact, so that the running balance updates immediately and accurately.As an Owner or Staff, I want to see a contact's balance update live if a teammate is also viewing it, so that we never work with stale numbers when both looking at the same ledger.As an Owner or Staff, I want to attach a photo of a receipt or handwritten chit to a transaction, so that I have proof of the transaction if it's ever disputed.As an Owner, I want to see who edited or deleted a transaction and what it looked like before, so that I can hold my team accountable and resolve disputes with confidence.As an Owner or Staff, I want to be blocked from editing a transaction someone else already has open, so that we never silently overwrite each other's changes.As an Owner or Staff, I want to search and filter transactions by date, type, or amount, so that I can find a specific entry quickly even with hundreds of records.As an Owner, I want to restore an accidentally deleted transaction within 7 days, so that a mistake doesn't mean permanently losing a financial record.As an Owner or Staff, I want to generate a shareable read-only link or PDF of a contact's ledger, so that I can send a customer their statement of dues over WhatsApp.As an Owner or Staff, I want to set a due date and get reminded, so that I don't lose track of when a payment is expected.Dashboard & ReportingAs an Owner, I want to see total dues, total payables, salary payable, and loss on one dashboard, so that I get the shop's full financial picture at a glance every morning.As an Owner, I want to see daily and monthly earnings without waiting for a page to recalculate, so that the dashboard stays fast no matter how much history the shop has.Import & SetupAs an Owner or Staff, I want to bulk-import contacts and opening balances from a CSV, so that I don't have to manually re-enter every existing customer and vendor.As an Owner or Staff, I want to belong to and switch between multiple shops, so that I can manage more than one shop location from a single account.PlatformAs an Owner or Staff, I want to install the app on my phone and keep viewing my ledger when offline, so that patchy network at the counter doesn't stop me from working.As an Owner or Staff, I want to have offline writes sync automatically when I'm back online, so that I don't lose entries made while disconnected.As an Owner or Staff, I want to switch the app language between Hindi and English, so that I can use the app comfortably in the language I'm most fluent in.6.2 Extended Module User StoriesWorker & Salary ManagementAs an Owner, I want to add a worker with a monthly salary or a daily wage rate, so that salary calculations match how I actually pay my staff.As an Owner or Staff, I want to mark daily attendance (present/absent/half-day/leave), so that pending salary is calculated accurately without manual math.As an Owner, I want to record a salary payment against a worker, so that the pending salary payable figure stays accurate and auditable.Inventory & Loss TrackingAs an Owner or Staff, I want to add products and stock batches with expiry dates, so that I can track what's in the shop and when it will expire.As an Owner or Staff, I want to log a purchase or sale and have stock update automatically, so that I don't have to separately reconcile stock counts.As an Staff, I want to submit a stock adjustment or loss entry for Owner approval, so that errors or shrinkage get a second set of eyes before they affect official stock counts.As an Owner, I want to approve or reject a submitted stock adjustment or loss, so that I retain control over changes that could hide shrinkage or error.As an Owner, I want to get notified before a batch of stock expires, so that I can sell or discount it before it becomes a total loss.AI-Assisted Paper Khata MigrationAs an Owner or Staff, I want to photograph pages from my old paper khata, so that I don't have to manually retype years of handwritten records.As an Owner or Staff, I want to review AI-extracted names and balances before anything is saved, so that I catch extraction errors before they enter my real ledger.As an Owner or Staff, I want to export the reviewed data as a CSV even if I don't commit it, so that I have a digitized backup of my paper records regardless.7. System Architecture & Data Workflow (End-to-End)Hisab Kitab is a single Next.js codebase covering both frontend and backend — there are no separate frontend/backend repositories. The boundary between the two is enforced by import direction rather than by folder split: UI components never import backend logic directly; API routes are the only bridge between them.7.1 Request FlowA user action in the UI (components/) calls an API route (app/api/.../route.ts).The route parses and validates the request against a shared Zod schema (server/validators/) and calls a function in server/services/ — routes contain no business logic themselves.The service function orchestrates the use case (e.g. locking, version checks, rollup updates) and calls server/repositories/ for any database access — Prisma is only ever called from the repository layer.The response flows back through the route to the UI.7.2 Real-Time Sync (Live Balances)When a transaction is written, PostgreSQL emits a LISTEN/NOTIFY event. A server-side listener (server/realtime/pg-listener.ts) picks this up and bridges it to a Server-Sent Events (SSE) stream (app/api/sse/). Any open browser tab subscribed via the useSSE hook receives the update and re-renders the balance live — without polling and without a page refresh.7.3 Concurrency & Data IntegrityEvery transaction edit is protected by a per-contact lock (server/lib/with-contact-lock.ts) so two people cannot edit the same contact's ledger at the same instant.Transactions carry a version number; if two edits race, the second one is rejected with a conflict rather than silently overwriting the first.Deletions are soft (a deletedAt flag), never a hard row delete — this is what makes the audit trail and 7-day restore window possible.Monetary values are stored as fixed-precision decimals (never floats), to avoid rounding errors in balances.Contact balances are maintained as a rollup (a running total), recalculated defensively by a scheduled job (server/jobs/) to self-heal against any drift, rather than relying purely on incremental updates.7.4 Background JobsExpiry-check (inventory batch expiry alerts) and rollup-repair jobs run on a schedule via app/api/cron/ routes, secured by a shared secret and currently triggered by the free external scheduler cron-job.org.7.5 AI-Assisted Migration FlowA user photographs paper khata pages in-app.Images are sent to Gemini (server/lib/gemini.ts) for extraction of contact name + final balance per page.Extracted data is shown to the user for mandatory review and correction — nothing is written to the real ledger until the user explicitly confirms.On confirmation, contacts and opening balances are created through the same validated service/repository path as any other contact creation.7.6 Offline Support (PWA)The app is installable and usable offline for viewing already-synced data.Writes made offline are queued locally and flushed to the server once connectivity returns, going through the same validation and conflict-check path as an online write.8. Core Architectural Decisions & Working AssumptionsThese are the foundational decisions the technical design already rests on. They are stated here as context for anyone reading this PRD, including where the team is deliberately relying on an assumption rather than a guarantee.Locking over optimistic concurrency for transaction editsA pessimistic, per-contact lock is used instead of pure optimistic (version-only) concurrency, because a shop's transaction volume is low enough that lock contention is rare, and the simplicity of 'you can't even open the edit form while someone else is in it' is easier for non-technical users to understand than a conflict-resolution screen.Server-Sent Events over WebSocketsAll real-time requirements in this product are one-directional (server pushes an updated balance to the browser); no client-to-client messaging is needed. SSE is simpler to operate on Cloud Run than a persistent bidirectional WebSocket connection, and is sufficient for this use case.Soft delete, not hard deleteEvery deletable record (transactions, etc.) is soft-deleted with a 7-day restore window, satisfying the audit-trail requirement without needing a separate archive system.Fixed-precision decimal moneyAll monetary fields use fixed-precision decimals rather than floating-point numbers, to avoid rounding errors accumulating across thousands of transactions.Rollup balances with self-healing repairContact balances are stored as a running rollup for fast reads, with a background job that recalculates from the transaction history to correct any drift — this assumes the rollup-repair job runs reliably; if the underlying scheduler is delayed or fails, balances could drift until the next successful run.Free-tier dependencies (working assumptions, not guarantees)cron-job.org (free) is used to trigger scheduled jobs in v1, with a documented plan to migrate to GCP Cloud Scheduler later. This assumes cron-job.org's reliability is acceptable for expiry-alert and rollup-repair timing in v1; it is not a guaranteed-uptime service.Gemini's free tier is used for paper khata extraction. This assumes free-tier rate limits and accuracy are sufficient for expected v1 usage volumes; both should be revisited if usage grows or extraction quality is inconsistent.Dataset availability for AI migration⚑ Open item: Sample paper-khata images (or any real transaction data) for testing the AI extraction flow have not yet been confirmed available with a data/engineering team. This should be resolved before the extraction accuracy work begins, since extraction quality cannot be properly validated without representative samples.9. Extended Module Specifications9.1 Worker & Salary ManagementTracks shop staff separately from ledger contacts, covering attendance and pay.Add a worker with either a fixed monthly salary or a daily wage rate.Mark daily attendance: present, absent, half-day, or leave.Pending salary is calculated from the attendance record and pay type, shown as 'Salary Payable' on the dashboard.Record salary payments, which reduce the payable amount and are themselves auditable (same soft-delete/edit-trail approach as ledger transactions).9.2 Inventory & Loss TrackingTracks stock at the product/batch level, tied to purchases and sales, with a deliberate approval step for anything that reduces stock unexpectedly.Products and stock batches carry quantity and expiry date.Purchases and sales automatically move stock in the corresponding direction.Any adjustment or loss entry submitted by Staff requires Owner approval before it affects official stock counts — this is a deliberate control point to catch errors or shrinkage before they're silently absorbed into the numbers.A scheduled job checks for upcoming batch expiries and raises an in-app notification ahead of time.9.3 AI-Assisted Paper Khata MigrationAimed at the biggest adoption barrier for paper-ledger shopkeepers: migrating existing history without a large manual data-entry effort.v1 scope: extract contact name and final balance per page — sufficient to seed opening balances for existing contacts.Every extraction is shown to the user for review and correction before anything is committed to the real ledger; nothing is auto-committed.Users can export the reviewed extraction as a CSV even without committing it to their live ledger, as a standalone digitization benefit.Dated line-item extraction (recreating each individual historical transaction with its original date) is deferred beyond v1 — see Section 5.3, Out of Scope.10. Role-Based Permissions MatrixConsolidated from the roles and access rules described across the application flows.CapabilityOwnerStaffAdd / edit contacts & transactionsYesYesView financial totals (dues, payables, dashboard)YesConfigurable by OwnerDelete a transactionYesConfigurable by OwnerRestore a deleted transactionYesConfigurable by OwnerView full audit trail (who changed what, when)YesNoManage shop settings, roles, ShopMembersYesNoApprove stock adjustments / lossesYesNoSubmit stock adjustments / lossesYesYesRecord salary paymentsYesConfigurable by OwnerMark worker attendanceYesYesBulk CSV importYesYesBelong to / switch between multiple shopsYesYes11. Appendix — Project Folder StructureReference structure for engineering; included here for traceability between this PRD and the codebase, per the Project Folder Structure document.Guiding principlesFrontend/backend separation lives inside a single src/ tree (not two repos), enforced by import direction: components/ never imports server/ directly — API routes are the only bridge.Folders below the first level are organized by feature/domain (contacts, transactions, workers, inventory, paper-import), not by technical type — matching the entities in this PRD so a developer working on one feature mostly stays within one set of folders.Thin routes, fat services: API route files only parse the request, call a service function, and return the response. All logic lives in server/services/, testable independently of HTTP.server/repositories/ is the only layer that calls Prisma directly for core entities, so schema changes have a contained blast radius.One Zod validator file per resource, shared by both the API route and the client-side form, so server and client validation never drift apart.Docker Compose provides a one-command local setup (Postgres + app with hot reload) for onboarding, and a prod-shaped Compose file that CI builds against to catch environment drift early.Tests mirror the source tree 1:1, so it's always obvious where a test for a given file lives.Top-level layoutsrc/app/ — Next.js routes only (route groups for auth vs. authenticated dashboard; app/api/ for backend endpoints, including sse/ and cron/)src/components/ — presentation only, split into ui/ (dumb primitives), layout/ (shell/nav), and features/ (feature-scoped components)src/server/ — all business logic: services/, repositories/, validators/, middleware/, realtime/, jobs/, lib/src/hooks/, src/lib/, src/i18n/, src/config/, src/types/ — shared client-safe utilities, translations, feature flags, and shared typesprisma/ — schema, migrations, seed scriptdocker/, infra/, .github/workflows/ — containerization, GCP/cron infra notes, and CI/CD pipelinestests/ — unit, integration (real Postgres via Docker), and e2e (Playwright), mirroring src/ structure